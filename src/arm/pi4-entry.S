// ARCBOOT v0 for pi4b (armv8, 64bit)

// for pi4b, bios mmio is setup on the fly (hotplug)
// ACPI can be implemented to

// after boot, should mount the kernel's root filesystem pointed
// to by root_fs_partition